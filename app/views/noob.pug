extends ./layout

block content
  if error
    h1.red #{error}
  else if msg
      h1.green #{msg}
  else if secureInput
      h1.green !{secureInput}
  else
    if nope
      h1.red #{nope}
    else 
      h1 Hey! You can upload some files here ...
  form(method='POST' action="/upload?level=noob" enctype="multipart/form-data")
    input(type='file' name='avatea' id='file')
    p Drag your files here or click in this area.
    button(type='submit') Upload
  br
  if files
    for file in files
      img(src!='static/' + uid + '/' + file)
